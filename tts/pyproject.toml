[project]
name = "soniox-tts-compare"
version = "0.1.0"
description = "Side-by-side text-to-speech comparison across providers."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "google-auth>=2.40.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.1.0",
    "requests>=2.32.0",
]
